apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "caesar"
version = '1.0'
mainClassName = "Caesar"

repositories {
  mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}